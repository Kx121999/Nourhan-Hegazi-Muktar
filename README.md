# Nourhan Cinematic Portfolio

Upload `index.html` to the root of a GitHub repository and enable GitHub Pages. The three campaign screenshots are embedded directly inside the HTML. Arabic is the default language and the EN button switches to English.
